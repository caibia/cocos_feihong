# 编辑器改动检查模板

适用于 `tools/editor-*/`、`tools/editor-common/` 与 `doc/*编辑器完整方案.md` 相关改动。

## 1. 变更摘要

| 项目 | 内容 |
| --- | --- |
| 编辑器名称 |  |
| 改动类型 | 修复 / 扩展 / 重构 |
| 影响目录/模块 |  |
| 影响字段 |  |
| 目标结果 |  |

## 2. 字段变更清单

| 字段名 | 变更类型 | 旧类型 | 新类型 | 默认值 | 兼容说明 |
| --- | --- | --- | --- | --- | --- |
|  | 新增/删除/改名/类型变化 |  |  |  |  |

补充要求：命中 `ID 自动生成只读`、`图标选择器 + 预览字段只读展示`、`options.json 静态类型同步导出 Enum` 时，必须在“兼容说明”中明确写明是否“不做兜底兼容”，以及对应 Enum 导出文件路径；若因缺少 `schema/options.json` 或无静态选项而跳过，也需写明跳过原因。

## 3. 文件级修改清单

| 层级 | 目标文件 | 是否已改 | 说明 |
| --- | --- | --- | --- |
| schema | `config/*-schema.json` | 否 |  |
| 选项 | `config/options.json` | 否 | 涉及静态下拉、类型定义时必查 |
| UI | `editor.js` | 否 |  |
| 校验 | `scripts/validator.js` | 否 |  |
| 联表 | `scripts/reference-service.js` | 否 |  |
| 枚举 | `tools-export/enums/*.ts` | 否 | 涉及 `options.json` 导出 Enum 时必查 |
| 服务 | `server.js` | 否 | 涉及本地服务、导出接口、文件读写时必查 |
| 回归 | `regression/*` / `scripts/run-*.js` | 否 | 页面夹具、冒烟脚本、回归脚本 |
| 共享 | `tools/editor-common/*` | 否 | 涉及公共组件、样式、回归基座时必查 |
| 说明 | `README.md` | 否 | 使用方式、命令、目录路径 |
| 文档 | `doc/*编辑器完整方案.md` | 否 |  |

### 3.1 目录与命名一致性

| 检查项 | 是否已确认 | 备注 |
| --- | --- | --- |
| 编辑器目录是否使用 `tools/editor-*/` 命名 | 是/否 |  |
| 如涉及共享模块，引用路径是否指向 `tools/editor-common/` | 是/否 |  |
| `run.cmd`、`stop.cmd`、`README.md`、完整方案文档中的目录路径是否与实际一致 | 是/否 |  |
| 改名、删字段或迁移目录后，是否已清理旧路径/旧名称残留引用 | 是/否 |  |

## 4. 显示与存储一致性

| 字段 | UI 显示值 | 实际存储值 | 是否分离 | 备注 |
| --- | --- | --- | --- | --- |
|  |  |  | 是/否 |  |

### 4.1 强制规则专项检查

| 检查项 | 是否已确认 | 备注 |
| --- | --- | --- |
| `ID` 字段是否自动生成且不可编辑，且未保留兜底/兼容逻辑 | 是/否 |  |
| 使用“图标选择器 + 预览”的字段是否改为只读展示，且未保留手填输入 | 是/否 |  |
| `config/options.json` 中静态下拉/类型定义是否已集成到导出DB功能 | 是/否 |  |
| 是否已同步导出到 `tools-export/enums/` | 是/否 |  |
| Enum 文件命名是否符合 `EXxx.ts` 规则（如 `EItem.ts`） | 是/否 |  |
| `enum` 命名是否符合 `EItemType` 形式 | 是/否 |  |
| `enum` 与枚举成员是否都已补充中文注释 | 是/否 |  |
| 若当前编辑器缺少 `schema + options.json` 或无静态选项，是否已明确记录“跳过 Enum 导出”原因 | 是/否 |  |

### 4.2 图片/图标类字段专项检查

| 检查项 | 是否已确认 | 备注 |
| --- | --- | --- |
| 是否使用“图标选择器 + 只读展示 + 预览”交互 | 是/否 |  |
| 是否未保留手填路径输入、旧控件兜底或兼容分支 | 是/否 |  |
| 如字段允许为空，schema 与文档中是否明确标注“可空” | 是/否 |  |
| 保存值是否统一为相对于 `resources` 目录的路径 | 是/否 |  |
| 保存值是否去掉文件扩展名 | 是/否 |  |

### 4.3 关联表 ID 字段专项检查

| 检查项 | 是否已确认 | 备注 |
| --- | --- | --- |
| 关联表 ID 字段是否做到“显示值与存储值分离” | 是/否 |  |
| 单选场景是否尽可能避免手动输入，并优先提供弹窗选择/联表单选器 | 是/否 |  |
| 单选场景是否显示关联表 `name` 字段内容 | 是/否 |  |
| 多选场景是否使用“操作选项弹窗多选器” | 是/否 |  |
| 多选弹窗风格是否与资源选择弹窗一致 | 是/否 |  |
| 多选弹窗是否支持按关联表 `name` 字段内容搜索 | 是/否 |  |
| 最终落库是否仍保存 ID 或 ID 数组 | 是/否 |  |
| 如出现“跨表 ID + 数量”组合，是否尽可能归并为一个结构化字段（如 `{ [id]: num }`） | 是/否 |  |

## 5. 最小验证记录

| 验证项 | 命令/动作 | 结果 | 备注 |
| --- | --- | --- | --- |
| JS 语法检查 | `node --check <file>` | 通过/失败 |  |
| 字段残留检查 | `rg \"<字段名>\"` | 通过/失败 |  |
| 路径/命名残留检查 | `rg "editor-|editor-common|<旧路径>"` | 通过/失败 |  |
| Enum 导出检查 | 检查 `tools-export/enums/<文件名>.ts` 路径、命名、注释与导出内容 | 通过/失败 | 涉及 `options.json` 或静态下拉改动时必查 |
| 合法样例 | 保存合法数据 | 通过/失败 |  |
| 非法样例 | 保存非法数据 | 通过/失败 |  |

## 6. 交付输出格式

1. 结果摘要（本次改了什么、解决什么问题）。
2. 文件级改动点（按文件逐条列出，必要时附路径与行号）。
3. 验证结果（已执行/未执行）。
4. 风险与后续建议（可选）。
