利用：https://github.com/OpenFairyGUI/OpenFairyGUI
fui路径：导出的资源素材\res\common\ui\
源工程路径：资源\UIProject

把 login 转为 源工程
源工程里如果没有依赖包，则把依赖的包 也导出源工程，依赖包只放依赖的图片、组件 其余的先不放 后面在有依赖的时候在补进来

在结合 
1.录屏：FGUIProject/登录.mp4 视频文件、
2.旧代码：导出的资源素材\src
3.ui源工程
4.本项目的框架
给我一份详细的 符合本项目框架规范的 登录-计划开发文档 存放到 docs下的 计划开发文档 目录下


Windows 原生 EXE 里，当前 cc.VideoPlayer 方案不能用，不是再调层级就能解决。
Creator 3.8.8 源码明确限制：
VideoPlayer 只支持 Web、iOS、Android。
stayOnBottom 只对 Web 有效。
原生 CMake 在非移动平台直接执行 USE_VIDEO OFF。
Windows 分支没有编译任何 VideoPlayer 实现。
所以 ENABLE_TRANSPARENT_CANVAS 对 Windows EXE 完全无效，修改底层渲染器也解决不了“没有 Windows 视频解码后端”这个根因。
建议的原生方案是“视频解码为 Cocos 纹理”：
login01.mp4
    ↓ Windows Media Foundation 解码
BGRA 视频帧
    ↓ 上传 Texture2D
Cocos Sprite 背景
    ↓
按钮、文本等普通 Cocos UI
这样视频和 UI 都进入同一套 Cocos 渲染管线：
背景 Sprite 层级 0
UI 层级 10
不需要透明窗口
不需要 stayOnBottom
UI 一定可以盖在视频上
继续使用现有 login01.mp4
当前视频静音，因此暂时不需要处理音频同步
实现上应增加项目级 Windows 原生插件，而不是修改 Creator 安装目录：
使用 Windows Media Foundation 的 IMFSourceReader 解码 H.264 MP4。
C++ 维护 2～3 帧循环缓冲，解码线程不阻塞游戏线程。
JSB 暴露 open/play/stop/update/loop。
每帧把最新 BGRA 数据上传至 Cocos Texture2D。
LoginView 在 Windows 原生创建视频纹理 Sprite，Web 继续使用当前 VideoPlayer。
页面关闭时停止线程、释放解码器和纹理。
另外两个方案：
FFmpeg 视频纹理：兼容性更强，但要附带 DLL、处理许可证和更大的安装包。
Web 构建套 Electron：改动最少且现有方案直接可用，但它不是 Cocos 原生 EXE。
针对这个 10 秒、无声、循环登录背景，我推荐 Windows Media Foundation 视频纹理方案。它不需要附带 FFmpeg，也不用碰 Cocos 底层渲染器。这属于新增 Windows 原生插件，不是简单改 LoginView；确认采用这个方案后再进入具体实现。